import React from 'react';

export const Home = () => {

  // const [session] = useContext(SessionContext);
  // console.log({ session })
  
  // if (!session) {
  //   return <Navigate to="/login" replace />
  // }

  return (
    <>
      <div>Home</div>
    </>
  );
};
